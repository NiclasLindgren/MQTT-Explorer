## Session: 2026-03-22 - Build Fixes, UI Improvements, and Dark Mode Fixes

### Build & Dev Environment

- Fixed Node engine incompatibility by using `--ignore-engines` flag (project targets Node 16, dev machine runs Node 24)
- Resolved broken `react-ace@14.0.1` npm tarball by downgrading to `react-ace@13.0.0`
- Fixed Windows dev script: changed `node_modules/.bin/webpack-dev-server` to `npx webpack-dev-server` in `app/package.json`
- Fixed webpack compilation warnings:
  - Changed `backend/src/Model/index.ts` to use `export type` for type-only exports (Message, Hashable, MessageHistory, TopicDataType)
  - Added `ipcRenderer` and `ipcMain` stubs to `app/src/mocks/electron.ts` for browser build

### Connection Setup UI - Advanced Settings Layout

- Refactored `AdvancedConnectionSettings.tsx` to use flex column layout so the topic subscription list expands to fill available space
- Changed `Subscriptions.tsx` from fixed `height: 196px` to `height: 100%` with overflow auto
- Modified `ConnectionSetup.tsx` to use conditional rendering (instead of MUI Collapse) for advanced settings, enabling unbroken flex height propagation
- Aligned Client ID text field and Certificates/Back buttons to bottom using `alignItems="flex-end"` on the Grid container
- Added 12px padding between subscription list and bottom row

### Connection Profile List - Search Filter

- Added a search/filter TextField with search icon to `ProfileList/index.tsx`, positioned below the "Add Connection" button
- Filters connections by name or host as user types
- Arrow key navigation operates on the filtered list

### JSON Diff Icon Fixes

- Fixed +/- icon sizing in `Gutters.tsx`: switched from `width`/`height` to `fontSize: '16px'` with `&.MuiSvgIcon-root` selector for proper MUI specificity
- Fixed chart preview icon in `ChartPreview.tsx` to match sizing
- Added `lineHeight: '16px'` to gutterLine style for proper vertical alignment

### Dark Mode Fixes

- Fixed Disconnect and Logout button text color in `TitleBar.tsx`: changed from `sx={{ color: 'primary.contrastText' }}` to `color="inherit"` (primary.contrastText resolves to black in dark mode because MUI's default dark primary is light blue)
- Fixed Pause icon color in `PauseButton.tsx`: changed from `theme.palette.primary.contrastText` to `'inherit'`

### Files Modified

| File | Change |
|------|--------|
| `CLAUDE.md` | Created project guidance |
| `app/package.json` | react-ace downgrade, dev script fix |
| `backend/src/Model/index.ts` | Type-only exports |
| `app/src/mocks/electron.ts` | IPC stubs for browser build |
| `app/src/components/ConnectionSetup/AdvancedConnectionSettings.tsx` | Flex layout, alignment |
| `app/src/components/ConnectionSetup/Subscriptions.tsx` | Dynamic height |
| `app/src/components/ConnectionSetup/ConnectionSetup.tsx` | Flex column, conditional rendering |
| `app/src/components/ConnectionSetup/ProfileList/index.tsx` | Search filter |
| `app/src/components/Sidebar/CodeDiff/Gutters.tsx` | Icon sizing/alignment |
| `app/src/components/Sidebar/CodeDiff/ChartPreview.tsx` | Icon sizing/alignment |
| `app/src/components/Layout/TitleBar.tsx` | Dark mode text color |
| `app/src/components/Layout/PauseButton.tsx` | Dark mode icon color |
